

export interface Option {
    questionNo: number,
    optionNo: number,
    optionText: string,
    icon: string
}

export interface QuestionMap{
    questionNo: number,
    questionText: string
}

export interface OptionMap {
    optionNo: number,
    optionText: string
}